# Discord完結型句会Bot 要件定義 v1

## 概要
Discord上で完結する句会システム。

前提:
- Discord Botベース
- Web UIなし
- Docker配布前提
- データベースはローカル(SQLite想定)
- 複数句会同時開催対応
- 暗号化は行わない
- 投句・選句は非公開（公開前は管理者・一般参加者とも閲覧不可）

---

# 1. 句会状態遷移

標準:

```
draft
↓
entry_open
↓
entry_closed
↓
submission_open
↓
submission_closed
↓
waiting_publish
↓
voting_open
↓
voting_closed
↓
waiting_results
↓
results
↓
ended
```

追加状態:

```
paused
cancelled
```

補足:
- エントリーは設定によりスキップ可能
- waiting_publish は手動公開時のみ
- waiting_results は手動結果公開時のみ
- 管理者は任意状態へ移動可能
- 後戻り時は公開メッセージ削除、入力済データは原則保持

---

# 2. エントリー

設定:

- エントリー有無
- 承認有無
- 最小開催人数
- 最小人数不足時:
  - 管理者判断（デフォルト）
  - 自動中止

人数判定:

承認あり:
- 承認済人数

承認なし:
- エントリー人数

エントリーなし時:
- 投句者全員が参加者

エントリー項目:

- 俳号（任意）
  - 空欄時はDiscordサーバ内表示名使用

参加状態:

```
pending
approved
rejected
withdrawn
```

特別参加者:

- 投句数/選句数等の制限を無視可能

エントリー取消:

- entry_open中のみ可能
- entry_closed以降は管理者のみ削除可能

---

# 3. 投句

設定:

投句数:

```
min
max
```

例:

```
1〜3
3固定
```

制約:

- 超過許可/禁止
- 不足許可/禁止

進行モード:

- 手動（デフォルト）
- 半自動
- 全自動

全自動時 / 不足参加者の投句:

- そのまま公開（デフォルト）
- 破棄

仕様:

- 締切まで編集可能
- 一部のみ入力可
- 保存途中状態可
- 締切時に確定

不足時:

通知:
- 対象者
- 管理者

通知タイミング:
- カスタマイズ可能

例:

- 24時間前
- 6時間前
- 1時間前
- 10分前

締切後（手動・半自動時）:

管理者:
- そのまま公開
- 破棄

半自動時:

- 全員が投句min以上を満たす場合のみ自動進行
- 不足者がいる場合は管理者判断

全自動時:

- 不足参加者の投句は作成時の設定に従い処理（そのまま公開 / 破棄）

---

# 4. 投句公開

句会チャンネル設定:

- 句会名で新規チャンネル作成（デフォルト）
- 既存チャンネルを指定

用途:

- 投句公開メッセージの投稿先
- 結果公開メッセージの投稿先
- 通知のデフォルト投稿先

---

設定:

投句締切後:

- 自動公開
- 手動公開

公開時:

- 投句番号ランダム割当
- 公開後編集不可
- 公開と同時に選句開始

---

# 5. 選句ルール

## 5.1 ルールテンプレート

デフォルト:

```
特選:2
並選:1
予選:0
```

各項目:

```
ラベル名
点数(点数制ON時)
順位優先度
```

順位優先度デフォルト:

- 点数制ON:
  - 点数降順
  - 同点時は定義順

- 点数制OFF:
  - 定義順

例:

```
特選:
 point=2
 rank=1

並選:
 point=1
 rank=2

予選:
 point=0
 rank=3
```

順位優先度は高度設定として変更可能。

テンプレート保存可

自句の選句は不可。作者コメントの追加を可とする（点数は0点、rank表示順は最後、他の選評と同じ形式で表示。）

---

## 5.2 句会ごとの選句数

例:

```
特選:1〜1
並選:3〜5
予選:0〜∞
逆選:0〜2
```

制約:

- 超過許可/禁止
- 不足許可/禁止

補足:

- 負の点数を持つ選句種別（逆選等）は通常の選句と同じルールで処理
- 特別扱いなし

---

## 5.3 選評

ラベルごとに設定可能:

```
なし
任意
必須
```

例:

```
特選:必須
並選:任意
予選:なし
```

句会全体への総評:

- 入力可能

---

## 5.4 選句進行設定

進行モード:

- 手動（デフォルト）
- 半自動
- 全自動

半自動時:

- 全員が選句要件を満たす場合のみ自動進行
- 不足者がいる場合は管理者判断

全自動時 / 不足参加者の選句:

- そのまま集計（デフォルト）
- 破棄

---

# 6. 結果集計

同点処理:

優先順位:

1. 高順位ラベル数
2. 次順位ラベル数
3. 以下同様
4. 投句番号順

例:

特選→並選→予選→番号

---

# 7. 結果表示

表示形式:

- 点数順
- 番号順
- 作者別

点数制OFF:

- 点数順非表示

デフォルト:

点数制ON:
- 点数順

点数制OFF:
- 番号順

作者別の場合の並び:

- 合計点数順
- 五十音順

作者内での句の並び:

- 点数順
- 番号順

作者公開:

設定:

- 公開する
- 公開しない

0点以下作者:

- 公開する
- 公開しない

判定:

```
score <= 0
```

---

# 8. Pause / Resume / 時刻処理

Pause:

- 任意状態で可能
- 受付、進行停止
- データ保持

Resume:

既定動作:

```text
管理者判断
```

Resume時:

未来の状態に対応する締切時刻を確認。

未来項目に過去時刻が存在する場合:

管理者へ再入力要求。

例:

```text
投句締切: 5/20 23:59
Resume: 5/21 02:00
```

表示:

```text
以下の締切が現在時刻を過ぎています:

- 投句締切
- 選句締切

新しい日時を入力してください
```

管理者選択:

- 日時再設定
- 即時進行

既定:

- 日時再設定

---

# 9. 通知

通知タイミング:

自由設定

例:

```
24h
6h
1h
10m
```

対象:

- 全体
- 未提出者
- 管理者

通知先:

- 句会チャンネル（デフォルト）
- 指定チャンネル
- DM

---

# 10. ボイス句会

進行とは独立イベント

設定:

- 使用しない
- 使用する

項目:

- 開始時刻
- 終了時刻
- 使用VC

既定:

```
選句締切 + offset
```

例:

+30分
+1時間
+2時間

VC:

- 共通VC

---

# 11. 権限

## サーバ単位: 句会作成権限

選択:

- 全員（デフォルト）
- 指定ロール
- Discord管理者
- サーバ所有者
- 指定ユーザー

指定ロール:

複数可

---

## 句会単位: 句会管理者

初期:

- 作成者

管理者を追加可能な人:

- 管理者
- サーバ所有者

権限:

- 締切変更
- 設定変更
- 中断
- 後戻り
- 公開
- 承認

管理者の削除: 

- 作成者
- サーバ所有者

---

# 12. コマンド体系（案）

基本方針:

- 参加者は原則 Slash Command + Modal
- 長い入力は Modal
- 選択系は Select Menu
- 個人入力系は非公開(ephemeral)
- 管理操作は句会管理者のみ
- コマンド数は最小化し、詳細操作はUI分岐

---

## 参加者コマンド

### 句会一覧

```text
/kukai list
```

表示:

- 開催中句会
- 募集中句会
- 自分の参加状況

---

### 詳細表示

```text
/kukai info
```

引数:

```text
句会
```

表示:

- 題
- 状態
- 締切
- 参加状況
- 投句状況
- 選句状況

---

### エントリー

```text
/entry
```

引数:

```text
句会
```

Modal:

```text
俳号
```

---

### エントリー取消

```text
/entry cancel
```

---

### 投句

```text
/submit
```

引数:

```text
句会
```

Modal:

```text
句
```

機能:

- 追加
- 編集
- 削除
- 一覧確認

---

### 選句

```text
/select
```

引数:

```text
句会
```

機能:

- 追加
- 編集
- 削除
- コメント入力
- 総評入力
- 一覧確認

---

### 自分の状態確認

```text
/check
```

引数:

```text
句会
```

表示:

```text
参加状態
投句数
選句数
不足状況
通知対象有無
```

---

### 結果表示

```text
/result
```

引数:

```text
句会
表示形式
```

表示形式:

```text
score
number
author
```

個人向け非公開表示

---

## 管理者コマンド

### 句会作成

```text
/kukai create
```

設定ウィザード開始

内容:

- 基本情報
- エントリー設定
- 投句設定
- 選句設定
- 通知設定
- 公開設定
- 進行設定
- ボイス設定

---

### 編集

```text
/kukai edit
```

引数:

```text
句会
```

変更対象:

- 締切
- 各種設定
- 選句ルール
- 管理者
- 通知

---

### 状態変更

```text
/kukai proceed
```

引数:

```text
句会
状態
```

例:

```text
submission_open
voting_open
results
```

---

### 一時停止

```text
/kukai pause
```

### 再開

```text
/kukai resume
```

---

### 公開

```text
/kukai publish
```

対象:

```text
投句一覧
結果
作者公開
```

---

### 後戻り

```text
/kukai rollback
```

機能:

- 公開メッセージ削除
- 状態巻戻し
- データ保持

投句一覧公開後の後戻り:

投句内容変更なし:

```text
公開メッセージ削除
↓
再公開
```

- 投句番号維持
- 選句保持

投句追加/削除/変更あり:

管理者へ警告:

```text
投句内容が変更されています。
既存の選句との整合性が失われる可能性があります。
```

管理者選択:

○ 選句保持
○ 選句リセット

選句保持:

- 既存submission_id紐付け維持
- 番号再シャッフル
- 既存選句・選評保持

選句リセット:

- 選句削除
- 選評削除
- 再シャッフル

### 承認

```text
/entry approve
```

```text
/entry reject
```

対象:

- 個別
- 一括

---

### 管理者追加

```text
/kukai admin add
```

### 管理者削除

```text
/kukai admin remove
```

---

### 中止

```text
/kukai cancel
```

---

# 13. 公開メッセージレイアウト

## 結果順 / 番号順

表示:

```text
## 8. 菜の花や月は東に日は西に

12点 ［特選5・並選3・予選1・逆選1］

[選評を見る]
```

仕様:

- 得点表示
- 各選句種別の人数表示
- 選評は初期非表示

「選評を見る」押下時:

本人のみ(ephemeral)表示

```text
## 8. 菜の花や月は東に日は西に

【特選】

浅野
- ○○。

池田
- ○○。

---

【並選】

坂本
- ○○。

---

【予選】

大野
- ○○。
```

並び:

- 選句種別順
- 種別内50音順

---

## 作者別表示

表示:

```text
# 山田太郎　計21点

## 8. 菜の花や……
12点 ［特選5・並選2］

## 5. 静かさや……
9点［特選3・並選3］
```

作者内並び:

- 点数順
- 同点時は番号順

---

# 14. 入力テキスト処理

目的:

- Discord Markdownによる表示崩れ防止
- 不要なメンション防止
- 将来のPDF等への出力互換性維持

保存時:

- ユーザー入力は生データのまま保持
- Unicode NFC正規化のみ実施

例:

```text
は + ゙
↓
ば
```

禁止:

- Markdownエスケープ済み文字列の保存

---

表示時:

Discord向け加工のみ実施

内容:

- Markdownエスケープ
- メンション無効化
- 元データは変更しない

例:

```text
春*雨*や
↓
春\*雨\*や
```

```text
@everyone 春の雨
↓
@​everyone 春の雨
```

対象例:

```text
*斜体*
**太字**
__下線__
~~打消し~~
# 見出し
> 引用
`コード`
||スポイラー||
@everyone
@here
ユーザメンション
チャンネルメンション
```

---

# 15. 実行環境・クラウド対応

目的:

- 特定クラウドへの依存を避ける
- 複数環境へ同一コードを配布可能にする

方針:

- Botはクラウド非依存設計
- 環境差分は環境変数のみで吸収

例:

```text
BOT_TOKEN=
DATABASE_PATH=
DATA_DIR=
```

想定環境:

- Docker
- VPS
- Railway
- Render
- Fly.io
- Raspberry Pi
- ローカルPC

保存:

- DB・生成物は永続Volumeへ保存

---

# 16. データエクスポート / インポート

管理者コマンド:

```text
/kukai export
/kukai import
```

エクスポート対象:

```text
○ 全データ
○ 指定句会
○ 指定期間
○ 作者別
```

出力形式:

```text
○ JSON
○ CSV
○ PDF（v1未実装）
○ SQLiteバックアップ
```

用途:

JSON:

- 環境移行
- バックアップ
- 将来インポート

CSV:

- Excel集計
- 分析

PDF:

- 記録
- 配布

SQLiteバックアップ:

- 完全復元

インポート:

- JSON
- SQLiteバックアップ

目的:

- クラウド間移行
- バックアップ復元
- 環境再構築

---

# 未整理事項

- DBスキーマ詳細
- Docker構成詳細

