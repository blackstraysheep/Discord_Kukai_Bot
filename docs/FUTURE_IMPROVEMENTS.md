# Kukai Bot 今後の改善項目（技術者向け）

最終更新: 2026-05-31

## A. 優先度高（運用直結）
- 公開ボタンメッセージの後編集対応
  - `persistent view` + `custom_id` による再起動耐性は実装済み
  - 将来、締切後に古いボタンを無効化したり文言を更新したりする場合は、公開時の `channel_id` / `message_id` を用途別に保存する
  - 保存したメッセージIDを使って、ステージ進行・締切・取消時に対象メッセージを編集する運用にする
- `kukai_id` 省略時解決の明確化
  - 「進行中」の定義を状態セットとして明文化
  - 同チャンネル複数句会時のUX改善（候補セレクタ提示）
- `/kukai edit` の高度な互換編集
  - 選句開始後に既存選句を保持したまま、ラベルIDを維持できる範囲で選句ルールの属性変更を許可する
  - 現状は `select_rule_config` による選句開始前の差し替え、または残存選句データ削除確認つき差し替え
- 通知ジョブ運用の可観測性向上
  - 「過去ステージ通知キャンセル」の件数・理由を運用ログで追跡可能にする

## B. 結果表示/公開
- 結果Viewの公開ポリシー統一
  - 自動公開メッセージの公開範囲・再表示ルールの定義を明文化
- 結果ページ構成の設定化
  - デフォルト表示形式（点数順/番号順/作者別）
  - 総評ページの挿入位置/ON-OFF
- 作者コメント表示戦略の明確化
  - どの形式に何件まで表示するか
  - 長文時の折りたたみ/別メッセージ化

## C. 選句UI
- 選句フォームの大規模句会対応
  - 句数>25時のページング選択UI
- 総評入力導線の補助
  - 未入力時の警告、完了時の確認導線強化
- 作者コメント専用UX
  - 自句選択時にラベル選択を省略し、即コメント入力へ遷移
- 登録完了通知の粒度調整
  - 大規模句会でのスナップショット表示件数/圧縮ルールを設定化

## D. 投句UI
- 投句GUIモーダル上限（5句）の改善
  - `/submit_bulk` は実装済みのため、GUI経路のみ段階的追加（次ページ）などで体験改善
- 投句下書き/再編集サポート
  - ユーザー側の作業中断耐性向上
- 注記表現改善
  - 「GUI上限」「句会上限」「現在登録数」の同時可視化
- 登録完了通知の表示調整
  - 抜粋行数・表示順・長文省略ルールの明確化

## E. ウィザード/設定管理
- 句会作成ウィザードのバリデーション強化
  - ステップ跨ぎ依存条件を中央集約（service側にも二重防御）
- ステップ構成の設定駆動化
  - フロー変更時にUI差分を最小化
- チャンネル作成失敗時の復旧フロー整備
  - 権限不足や名前重複のガイドをさらに具体化

## F. 同期/運用
- コマンド同期戦略の整理
  - 開発環境（guild sync）と本番（global sync）の運用手順を文書化
- 監視ログの構造化
  - ステージ遷移、通知送信、ボタン操作を追跡可能にする
- 障害時の再実行手順を整備
  - scheduler再登録、公開再送、ロールバック手順のプレイブック化

## G. テスト
- 回帰テスト拡充
  - ステート遷移
  - `kukai_id` 自動解決
  - 通知メッセージ内容（差分・締切・ボタン）
  - 結果公開のView整合（`/result` と同等）
- UIロジックのユニット化
  - Embed生成ロジックを純粋関数化しテスト容易性を上げる

## H. PDF生成

- 投句一覧のページあたり行数をコマンドオプション化
  - 現状は `theme.toml` の `submission.rows_per_page`（デフォルト30）で制御
  - `/pdf submission rows:25` のように実行時に上書きできるようにする
  - フォントサイズや余白変更時に自動計算できると理想的
- PDFキャッシュ（任意）
  - 同一 `(kukai_id, show_author, theme)` の組み合わせを一定期間保管し再生成を省略
  - データ変更時の無効化が必要なため、`/pdf submission --force` オプションとセットで検討
- 投句一覧のページ構成オプション追加
  - 現状: 固定行数でページ分割（tabular）
  - 将来: longtable（自動分割）との切り替えをテーマまたはコマンドで選択できるように

## I. 配布・インフラ

- バックアップ外部退避
  - VM内cronによるPostgreSQL dumpは `docs/operations.md` に整備済み
  - Oracle Free Tier/PAYG無料枠運用でもVM/boot volume喪失リスクがあるため、dumpを暗号化してVM外へ退避する
  - 候補: Oracle Object Storage（無料10GB）または管理者PCへの定期コピー
  - 外部退避後、別環境でのリストア演習手順も更新する
- GitHub Actions + GHCRによるDockerイメージ配布
  - セルフホスト配布を楽にするため、ビルド済みイメージをGHCR（GitHub Container Registry）にpush
  - `docker-compose.yml` を `build: .` から `image: ghcr.io/...` に切り替える
  - Watchtowerと組み合わせることでユーザー側の自動更新が可能になる
- Discord Bot Verification対応（75サーバー超時）
  - 公開Bot化する場合、75サーバー以上でDiscord側の審査が必要
  - プライバシーポリシー・利用規約ページの用意が前提条件
- Shard対応可能な設計
  - 現状は単一Botプロセス前提
  - データは `guild_id` / `channel_id` / `kukai_id` を保持しているため、将来のshard判定には利用可能
  - 将来shard化時はschedulerを専用worker化するか、job claim/lockを導入する
  - 通知・自動進行は対象guildを持つshardで実行できる設計にする

## H. ドキュメント
- 一括入力コマンドの利用例拡充
  - `/preset bulk`, `/kukai create_bulk`, `/submit_bulk`, `/select_bulk` の運用者向けサンプル集
- 開発者向け仕様同期
  - この `CURRENT_SPEC.md` と実装差異が出ないよう変更時更新を運用ルール化
